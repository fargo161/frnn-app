{
  "name": "frnn-app",
  "version": "2.0.0",
  "private": true,
  "description": "Reusable festival application platform for As Above So Below and Fried Rice News Network",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test-lab": "node scripts/start-test-lab.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "test:database": "node --env-file=.env.test.local --test",
    "qr": "node scripts/generate-qr.js",
    "codes:import": "node scripts/import-codes.js",
    "codes:generate": "node scripts/generate-codes.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1116.0",
    "express": "^4.21.2",
    "pg": "^8.13.1",
    "qrcode": "^1.5.4"
  }
}
