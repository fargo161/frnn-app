# Disposable automated PostgreSQL test settings. Copy to .env.test.local.
# Use a dedicated local database named frnn_integration_test. Tests create and
# drop only their own unique schemas inside it. Never use the owner Test Lab URL.
TEST_DATABASE_URL=postgres://TEST_USER:TEST_PASSWORD@127.0.0.1:55432/frnn_integration_test
